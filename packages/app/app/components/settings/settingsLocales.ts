/**
 * Language option type - sourced from /public/config.json at runtime.
 * Re-exported here so settings components have a single import point.
 */
export type { IClientConfigLanguage as LanguageOption } from '@/interfaces/IClientConfigLanguages';
